import { Timer } from "lucide-react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { uptimeData } from "../../data"
export function UptimeCard() { return <Card className="flex min-h-44 flex-col"><CardHeader><CardTitle className="flex items-center gap-2 text-sm font-medium"><Timer className="size-4 text-primary" />On Time</CardTitle></CardHeader><CardContent className="flex flex-1 flex-col justify-end gap-5"><strong className="flex items-baseline justify-center gap-1 text-5xl font-semibold tracking-tight tabular-nums"><span>{String(uptimeData.days).padStart(2, "0")}<small className="ml-0.5 text-xl font-medium text-muted-foreground">d</small></span><span className="text-foreground/75">{String(uptimeData.hours).padStart(2, "0")}<small className="ml-0.5 text-xl font-medium text-muted-foreground">h</small></span><span>{String(uptimeData.minutes).padStart(2, "0")}<small className="ml-0.5 text-xl font-medium text-muted-foreground">m</small></span></strong><div className="flex h-4 w-full items-end gap-1" aria-label="Histórico de uptime">{[3, 5, 4, 7, 6, 9, 8, 11, 10, 12, 9, 13, 11, 14, 12, 15, 13, 16].map((height, index) => <span key={index} className="min-w-0 flex-1 rounded-sm bg-primary" style={{ height: `${height}px`, opacity: 0.55 + (index / 18) * 0.45 }} />)}</div></CardContent></Card> }
